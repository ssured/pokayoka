import React from 'react';

import {
  Box,
  Flex,
  Text,
  Image as BaseImage,
  Button as BaseButton,
  Card as BaseCard,
} from '../base';

export const Card = BaseCard;
export const Button = BaseButton;
export const Image = BaseImage;

export const Grid = Flex;
export const List = Box;

export const Step = Flex;
export const Segment = Box;

export const Icon = Text;
