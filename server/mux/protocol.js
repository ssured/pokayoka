module.exports = {
  api: {
    //async is a normal async function
    hello: 'async',

    //source is a pull-stream (readable)
    stuff: 'source',

    //source is a pull-stream (readable)
    dbReplicationStream: 'source',
  },
};
